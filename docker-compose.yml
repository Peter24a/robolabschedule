version: '3.8'

services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///./robotics_lab.db
    command: sh -c "python init_db.py && streamlit run app.py --server.address=0.0.0.0"
